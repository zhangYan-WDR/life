<view class="page-shell launch-page">
  <view class="hero card">
    <view class="hero-title">家常日记</view>
    <view class="hero-desc">{{error || message}}</view>
    <button wx:if="{{error}}" class="primary-btn retry-btn" bindtap="retry">重新尝试</button>
  </view>
</view>
